"""Shared constants used across the ClusterSage codebase.

These values are intentionally small in number. Most behavior should be driven
by explicit configuration rather than hidden constants.
"""

APP_NAME = "ClusterSage"
DEFAULT_ENCODING = "utf-8"
DEFAULT_TIMEZONE = "UTC"
