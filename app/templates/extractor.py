"""Event template extraction entrypoints.

Template extraction groups normalized lines into recurring event shapes so a
human can reason about classes of behavior instead of reading the same message
variant hundreds of times.
"""
